# دليل إعداد النظام متعدد الأكاديميات

هذا المستند يشرح كيفية إعداد المنصة لخدمة عدة أكاديميات (tenants) من نفس النشر، مع عزل كامل للبيانات بين كل أكاديمية.

## ملخص الفكرة

كل أكاديمية = قاعدة بيانات MongoDB منفصلة + نطاق فرعي (subdomain) خاص بها.

- **الأكاديمية الافتراضية** (`default`) محفوظة على قاعدة البيانات الأصلية (`champions_academy`) بدون أي ترحيل.
- **أي أكاديمية جديدة** تنشئها من لوحة Super Admin تحصل على قاعدة بيانات مستقلة (`champions_<slug>`) ولا ترى ولا تتأثر ببيانات الأكاديميات الأخرى.

## كيف يحدد النظام الأكاديمية لكل طلب؟

ترتيب الأولوية في كل طلب HTTP:

1. **النطاق الفرعي (subdomain)** — مثال: `acme.champions.app` → الأكاديمية `acme`.
2. **رأس الطلب `X-Tenant-Slug`** — يستخدمه التطبيق الأمامي عند عدم توفر نطاق فرعي.
3. **الافتراضي** — إذا لم يحدد أي مصدر صريح، يُستخدم `default`.

> ملاحظة أمنية: إذا أرسل طلب نطاقاً فرعياً أو رأساً صريحاً غير معروف، يُرفض الطلب برمز **404** بدلاً من إرجاعه إلى الأكاديمية الافتراضية. هذا يمنع تسرب البيانات بين الأكاديميات.

## خطوات إعداد النطاقات الفرعية في الإنتاج

### 1. اختر نطاقك الأساسي
مثلاً: `champions.app` أو `myacademy.com`. هذا النطاق ستضع تحته كل الأكاديميات.

### 2. اضبط متغير البيئة `TENANT_BASE_DOMAIN`
في إعدادات Secrets على Replit، أضف:

```
TENANT_BASE_DOMAIN=champions.app
```

عند ضبطه، كل host بصيغة `<slug>.champions.app` يُترجم تلقائياً إلى الأكاديمية `<slug>`.
- `champions.app` و `www.champions.app` لا يُحلَّان كأكاديميات (يستخدمان `default`).
- `foo.bar.champions.app` (نطاقات فرعية متداخلة) لا تُحلّ كأكاديمية.

### 3. اربط النطاق بالنشر على Replit
- في صفحة **Deployments** بمشروعك، افتح **Custom Domains**.
- أضف النطاق الأساسي: `champions.app`.
- أضف **Wildcard subdomain**: `*.champions.app` (يلتقط جميع الأكاديميات).
- اتبع التعليمات لإضافة سجلات DNS اللازمة عند مزود النطاق:
  - سجل A أو CNAME للنطاق الأساسي.
  - سجل CNAME أو A wildcard `*` يشير إلى نفس وجهة النشر.
- انتظر بضع دقائق حتى يتم إصدار شهادات TLS تلقائياً لكل من النطاق الأساسي والـ wildcard.

### 4. أنشئ الأكاديميات من لوحة Super Admin
- ادخل إلى `https://champions.app/super/login` بحسابك الفائق.
- اضغط "+ أكاديمية جديدة" واملأ:
  - **Slug**: المعرف اللاتيني المستخدم في النطاق الفرعي (مثل `acme`، `albatal`).
    يجب أن يكون أحرفاً وأرقاماً فقط، بدون مسافات أو رموز.
  - **الاسم**: اسم العرض بالعربية.
  - **الباقة** (`starter` / `pro` / `enterprise`): تحدد الحد الأقصى للفروع والأعضاء.
  - **الميزات المفعّلة**: WhatsApp، تطبيق الموبايل، الناشر الاجتماعي، البطولات.

### 5. تحقق من الوصول
- افتح `https://acme.champions.app` → يجب أن تظهر صفحة دخول أداء الأبطال، لكن البيانات (الأعضاء، الفواتير، إلخ) ستكون فارغة لأن قاعدة البيانات جديدة.
- أنشئ أول مستخدم admin للأكاديمية الجديدة من شاشة التسجيل، ثم سجّل دخوله وابدأ بإضافة الفروع والأعضاء.
- يمكنك تكرار هذا لكل أكاديمية جديدة.

## بدون نطاق مخصص (للاختبار فقط)

إذا أردت تجربة عدة أكاديميات على نطاق Replit الافتراضي بدون نطاق مخصص:
- اترك `TENANT_BASE_DOMAIN` فارغاً.
- في التطبيق الأمامي، اضبط الأكاديمية الحالية في `localStorage` بمفتاح `tenant_slug`. مثال من Console المتصفح:

```js
localStorage.setItem('tenant_slug', 'acme');
location.reload();
```

سيُرسل التطبيق رأس `X-Tenant-Slug: acme` مع كل طلب.

## إيقاف أو حذف أكاديمية

من لوحة Super Admin:
- **إيقاف**: يمنع كل طلبات الأكاديمية فوراً (HTTP 403)، دون حذف بياناتها.
- **حذف**: يحذف سجل الأكاديمية من سجل التحكم. **بياناتها في MongoDB لا تُحذف تلقائياً** — لاستعادتها أعد إنشاءها بنفس الـ slug، ولحذفها فعلياً اتصل بقاعدة البيانات يدوياً واحذف `champions_<slug>`.

## حدود الباقات

عند تجاوز عدد الفروع أو الأعضاء المسموح للباقة، تُرجع الواجهة الخلفية:

```
HTTP 402 Payment Required
{ "detail": "تم بلوغ الحد الأقصى للأعضاء في باقتك" }
```

غيّر الباقة من زر "تعديل" في لوحة Super Admin لرفع الحد.

## ملاحظات أمنية

- رمز JWT لكل مستخدم مرتبط بأكاديميته (claim `tenant_slug`)، ولا يمكن إعادة استخدامه على أكاديمية أخرى حتى لو تم تغيير الـ subdomain أو الرأس يدوياً.
- بعد ترقية النظام، ستحتاج جميع الجلسات النشطة (admin والأعضاء) لإعادة تسجيل الدخول مرة واحدة لإصدار رمز جديد يحتوي على الـ tenant claim.
- لوحة Super Admin (`/super/*`) محمية بكلمة سر مستقلة تماماً (متغيرا `SUPER_ADMIN_USER` و `SUPER_ADMIN_PASSWORD`)، ولا يمكن استخدام رموزها على واجهات الأكاديميات `/api/*`.
